import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['img.daisyui.com', 'img.icons8.com', 'images.pexels.com', 'images.unsplash.com']
  }
};

export default nextConfig;
